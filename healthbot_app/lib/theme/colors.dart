import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFF7F4ED);   // paper/tag stock
  static const surface    = Color(0xFFFFFFFF);    // card surface
  static const ink        = Color(0xFF16191C);    // primary text
  static const inkMuted   = Color(0xFF6B6F76);     // secondary text
  static const border     = Color(0xFFD8D5CC);     // dashed/solid dividers

  static const riskLow    = Color(0xFF3B7A57);   // green
  static const riskMedium = Color(0xFFD9A03B);   // amber
  static const riskHigh   = Color(0xFFC23B3B);   // red — emergency only
}
